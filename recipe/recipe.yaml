context:
  name: altair-aitools-devkit
  version: 1.0b4

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://maven.rapidminer.com/repository/pypi-snapshot/packages/altair-aitools-devkit/${{ version }}/altair_aitools_devkit-${{ version }}.tar.gz
  sha256: c3a0c6425def4662820600a11825ea3c9dfc659387a7fe0b09d6653fd2d90aeb

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - build-extension = altair_aitools.ext._builder:build_extension

requirements:
  host:
    - python ${{ python_min }}
    - hatchling
    - pip
  run:
  - python >=3.9
  - networkx >=3.0,<4
  - pipdeptree >=2.20,<3
  - pyyaml >=6.0,<7
  - conda-index >=0.5.0,<0.6
  - libmambapy >=2.0,<3
  - altair-aitools-runtime >=1.0b5

tests:
  - python:
      imports:
        - altair_aitools.dev
  - requirements:
      run:
        - pip
    script:
      - pip check
      - build-extension --help

about:
  summary: altair-aitools-devkit is a comprehensive package that provides a set of tools to develop Altair RapidMiner AI tools extensions in Python
  license: AGPL-3.0-only
  license_file: LICENSE
  homepage: https://altair.com/altair-rapidminer

extra:
  recipe-maintainers:
    - skontar-altair
    - pczako-altair
